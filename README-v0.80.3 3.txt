B7 FI Command Center v0.80.3 — Live Status Responsive Layout Fix

Changes:
- Live Status now uses one shared centered responsive shell for the KLA+ header, status bars, summary metrics, and tools carousel.
- Lead Alerts and System Status align to the same left/right edges as the metrics and carousel.
- Removed legacy fixed/offset positioning from status bars while mounted in Live Status.
- Browser zoom and ultrawide/narrow window behavior now scale/reflow the complete Live Status display together.
- Keeps v0.80.2 read-only Tool Detail modal and carousel behavior.
- Version updated to v0.80.3.
