B7 FI COMMAND CENTER v0.79.0 — FRAMEWORK LOCK CANDIDATE

Changes in this build:
- Renames ACTION STATUS to LEAD ALERTS.
- Renames FLEET STATUS to SYSTEM STATUS.
- Uses one identical three-column geometry for both status bars.
- Makes the left and right divider positions pixel-identical between both bars.
- Centers both status messages geometrically and increases message size slightly.
- Prevents Center theme colors from driving status-bar severity colors.
- Keeps the existing status perimeter tracer treatment tied to each alert severity.
- Simplifies footer to Administration Center (left), KLA+ (true center), Local Production Mode + version (right).
- Removes footer Building 7 / Final Integration / Operations and SharePoint sync text.
- Keeps footer structurally after page content.
- Sets application version to v0.79.0.
- No MutationObserver or recurring JavaScript interval added.
