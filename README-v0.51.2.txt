B7 FI Command Center v0.51.2

Fixes / polish from the latest testing screenshots:

HEADER
- Three-zone layout:
  KLA+ black ~18%
  B7 FI COMMAND CENTER fixed blue ~30%
  active page color ~52%
- Laptop proportions adapt to ~15/33/52 and ~14/34/52.
- Only the active-page section changes color.

NAVIGATION
- Active navigation text is always white.
- Adds a global active-state sync using each nav button's data-view.
- Fixes FI Knowledge Base incorrectly highlighting Reference Files.
- Fixes Meeting Center incorrectly highlighting Administration.
- Applies the same active-state logic globally to all patched pages.

HOME
- All interactive Home live cards receive the same hover language as Tool Countdown:
  slight lift, accent-colored border/glow, brighter background and pointer cursor.

Version: 0.51.2
