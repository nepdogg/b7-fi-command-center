B7 FI Command Center v0.67.0
Shared Shell Stabilization Pass

- Fixed legacy MutationObserver toolbar reordering loops that could make page-navigation buttons unresponsive.
- Restored a persistent page-navigation bar to Operations Center.
- Removed the vertical gap between Action Status and page navigation.
- Rebuilt Action Status into a real three-part status bar with a tighter label section.
- Rebuilt Fleet Status with the same short-divider geometry and more message room.
- Forced Center-colored top and bottom status-bar borders and identical edge animations.
- Corrected footer width so it matches the Command Center frame instead of spanning the browser independently.
- Reset inherited body bottom padding and reinforced a full-height flex shell so short pages end at the browser bottom.
- Enlarged KLA/header titles while preserving header height.
- Preserved left-side page destinations and right-side page actions.
- Restored Screenshot and Report on normal Center pages.
- Search Center no longer shows a redundant ALL RESULTS button.
- Administration Center intentionally remains for the later dedicated redesign.
