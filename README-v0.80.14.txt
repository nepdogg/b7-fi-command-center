B7 FI Command Center v0.80.14

STATUS SYSTEM STABILIZATION
- Lead Alerts and System Status now use permanent shared shells.
- Rotating messages update data only; labels, beacon, dividers and counters are repaired if legacy renderers rewrite the bar.
- Severity is the only color source for border, dividers, lamp, category text and glow.
- System Status message uses the same larger, mathematically centered typography as Lead Alerts.
- Standalone Live Status viewer retains both Lead Alerts and System Status.
- Moving perimeter tracers remain disabled; calm severity glow remains.
- Critical popup behavior from v0.80.13 is preserved.
